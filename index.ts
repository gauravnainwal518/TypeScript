//welcome message
// A simple function to greet a user
function greet(name: string): string {
  return `Hello, ${name}! Welcome to TypeScript.`;
}

// Using the function
const userName: string = "Gaurav";
console.log(greet(userName));
